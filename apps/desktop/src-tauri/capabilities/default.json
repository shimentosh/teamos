{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "The window always loads TeamOS from the instance, so nothing local calls the shell. This exists only so the local origin is not left undeclared.",
	"windows": ["main"],
	"permissions": ["core:default"]
}
