{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "instance",
	"description": "TeamOS loaded from a known instance may start a browser sign-in, raise notifications and set the unread badge. It cannot read or change which server the app points at.",
	"windows": ["main"],
	"local": false,
	"remote": {
		"urls": [
			"https://teamos.sentosh.com",
			"http://localhost:5173",
			"http://127.0.0.1:5173"
		]
	},
	"permissions": [
		"allow-desktop-start-login",
		"allow-desktop-notify",
		"allow-desktop-set-badge",
		"allow-desktop-is-foreground"
	]
}
